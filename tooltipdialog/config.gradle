ext {
    libraryVersion = '1.1.4'
    libraryVersionCode = 6

    bintrayRepo = 'oss_android' // Repo name in bintray dashboard
    bintrayName = 'com.kcrimi.tooltipdialog' // package name of the bintray repo

    publishedGroupId = 'com.kcrimi.tooltipdialog'   // this is the ID we want to see in implementation line
    libraryName = 'tooltipdialog'     // this is the module name of library
    artifact = 'tooltipdialog'        // this is the artifact we want to see in implementation line

    libraryDescription = 'Easy to use tool tip dialogs with optional view highlighting' // description of library

    siteUrl = 'https://github.com/kcrimi/ToolTipDialog'    // git repo url
    gitUrl = 'https://github.com/kcrimi/ToolTipDialog.git' // git repo vcs url

    developerId = 'kcrimi' // This is your bintray username
    developerName = 'Kevin Crimi'              // Developer's name
    developerEmail = 'kevin@kevincrimi.com'                // Developer's email

    licenseName = 'The Apache Software License, Version 2.0'  // for example, The Apache Software License, Version 2.0
    licenseUrl = 'http://www.apache.org/licenses/LICENSE-2.0.txt'   // for example, http://www.apache.org/licenses/LICENSE-2.0.txt
    allLicenses = ["Apache-2.0"]    // array of licenses, for example, ["Apache-2.0"]
}